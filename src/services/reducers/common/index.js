// import { appActions } from "../../action";

const initialState = {};

export const commonReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};
