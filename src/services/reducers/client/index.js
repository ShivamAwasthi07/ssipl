// import { appActions } from "../../action";

const initialState = {};

export const clientReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};
